//1.-Primitive types
/*Create variables with explicit types for: name (string), 
age (number), student (boolean), and salary (number or null).*/

var nombre:string;
var edad:number;
var estudiante:boolean;
var salario:number | null;