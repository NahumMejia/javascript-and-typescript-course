//2.-Sum Function
/*Write a function that takes two numbers as parameters and returns their sum. 
Add types to the parameters and the return value. */

function sum(number1: number, number2: number):number {
    return number1 + number2;
}

sum(1,2);